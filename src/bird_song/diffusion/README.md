# Stage 5: Diffusion

Place the diffusion model, noise schedule, training loop, sampling, and evaluation modules here. Import the shared `bird_song.audio` preprocessing and `SpectrogramConfig`; do not create a second spectrogram implementation.
