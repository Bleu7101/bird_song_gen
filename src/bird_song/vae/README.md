# Stage 4: VAE

Place the VAE model, training loop, sampling, and evaluation modules here. Import the shared `bird_song.audio` preprocessing and `SpectrogramConfig`; do not create a second spectrogram implementation.
