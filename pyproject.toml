[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "bird-song-gen"
version = "0.1.0"
description = "Shared pipeline code for bird-song classification and generation"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
